# Extract Bruker ParaVision parameters from subject, acqp and method files

## Run tests
```
pip install -r requirements.txt
pip install pytest-cov

pytest test_extract_bruker.py --cov=extract_bruker
```
